machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker version

test:
  override:
    - docker run -t -v `pwd`:/test_ansible jdeathe/centos-ssh:centos-6 sh /test_ansible/vagrant-ansible/provision.sh /test_ansible/ansible

